## 2024-05-19 - Initial Analysis
**Learning:** The project is a static HTML/CSS site without a build step or package.json, so standard npm/pnpm commands don't apply. Default focus states are not styled, which makes keyboard navigation visually inconsistent.
**Action:** Implemented clear, customized `:focus-visible` styling to match the site's design system (blue theme, rounded corners).
